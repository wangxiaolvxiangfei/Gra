@model Graduation.Models.AcademyViewModel
@{
    Layout = "~/Views/Shared/Guide.cshtml";
}
@if (ViewBag.type == "1")
{
    ViewBag.type = "display:none";
}

<div class="templatemo-content col-1 light-gray-bg">
    <div class="templatemo-top-nav-container">
        <h2>求职与就业信息管理系统</h2>
        <div class="right">
            <span>欢迎&nbsp;&nbsp;</span><span class="admin">就业中心管理员：</span><span class="admin">Admin</span><span>&nbsp;&nbsp;登录本系统</span>
        </div>
    </div>
    <div class="templatemo-content-container now-position">
        <span>当前位置：</span><span>系统设置>></span><span>院系及专业名称管理>></span><span>学院名称管理</span>
        <h1 class="ly-title">学院名称管理</h1>
        @using (@Html.BeginForm(null, null, null, FormMethod.Post, new { @class = "form-inline ly-form" }))
        {
            @Html.HiddenFor(model => model.Academy.Id)
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)
            <div class="form-group">
                <label for="exampleInputAmount">学院名称：</label>
                <div class="input-group">
                    @Html.TextBoxFor(model => model.Academy.Name, new { @class = "form-control indexInput", placeholder = "学院名称", required = "required" })

                </div>
            </div>
            <button type="submit" class="btn btn-primary">添加或更新</button>
        }
        <div class="panel panel-default table-responsive">
            <table class="table table-striped table-bordered templatemo-user-table table-hover .houtai-table select">
                <thead>
                    <tr>
                        <th>学院ID</th>
                        <th>学院名称</th>
                        <th>选择</th>
                        <th>删除</th>
                    </tr>
                </thead>
                @foreach (var temp in Model.AcademyList)
                {
                    <tr>
                        <td class="index">@temp.Id</td>
                        <td>@temp.Name</td>
                        <td>@Html.ActionLink("选择", "AdminAcademy", new { Id = temp.Id })</td>
                        <td>@Html.ActionLink("删除", "AdminAcademy", new { Id = temp.Id, type = 1 })</td>
                    </tr>
                }
            </table>
        </div>
        <div class="right page">
            <a href="#">首页</a>
            <a href="#">&lt;&lt;</a>
            <a href="#" class="active">1</a>
            <a href="#">2</a>
            <a href="#">3</a>
            <a href="#">4</a>
            <a href="#">5</a>
            <a href="#">...</a>
            <a href="#">&gt;&gt;</a>
            <a href="#">尾页</a>
        </div>
        <footer class="text-right"></footer>
    </div>
</div>